# Issue #3 Builder-Attached Bundle Entrypoint

Use this note when the current headed localhost replay should stay pinned to the three builder-attached compatibility pages that are already present in `agent_files/`, and you want the shortest bridge into the existing issue `#3` attached-page helper chain without retyping the same filenames by hand.

This note matches:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\windows\show_google_issue3_builder_attached_bundle_entrypoint.ps1
```

## Current builder-attached bundle

Treat the current builder-attached compatibility set as these exact saved pages:

1. `Control your online safety and privacy – Google Safety Centre (09_05_2026 21：23：40).html`
2. `Job Application for [Expression of Interest] Research Manager, Interpretability at Anthropic (09_05_2026 21：25：29).html`
3. `Presidential Unsealing and Reporting System for UAP Encounters _ U.S. Department of War.html`

Keep the Google Safety Centre page first when the replay should stay aligned with the narrower issue `#3` Google-shaped follow-up route.

## Goal

Start from one helper that resolves the current builder-attached pages automatically from the repo workspace, then reprint the existing replay-attached quickstart, bundle-first route, bundle runner, proof entrypoint, and fixed-list proof commands with those exact files already pinned.

Use that helper when:

- the next replay should stay on the current builder-attached three-page bundle
- you want the replay to preserve the same exact filenames end to end
- the current run should avoid rediscovering or manually retyping the builder-attached inputs
- the next follow-up still needs the broader attached-page and Google-shaped widen-back-out helpers visible after the pinned bundle route

## Read-first route

Use this compact route when the builder-attached files are already present and no broader router rediscovery is needed first:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\windows\show_google_issue3_builder_attached_bundle_entrypoint.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\windows\show_google_issue3_windows_replay_attached_html_quickstart.ps1 -PreferredInitialPage 'Control your online safety and privacy – Google Safety Centre (09_05_2026 21：23：40).html' -InputPath 'Control your online safety and privacy – Google Safety Centre (09_05_2026 21：23：40).html' 'Job Application for [Expression of Interest] Research Manager, Interpretability at Anthropic (09_05_2026 21：25：29).html' 'Presidential Unsealing and Reporting System for UAP Encounters _ U.S. Department of War.html'
powershell -ExecutionPolicy Bypass -File .\scripts\windows\show_google_issue3_attached_bundle_first_entrypoint.ps1 -PreferredInitialPage 'Control your online safety and privacy – Google Safety Centre (09_05_2026 21：23：40).html' -InputPath 'Control your online safety and privacy – Google Safety Centre (09_05_2026 21：23：40).html' 'Job Application for [Expression of Interest] Research Manager, Interpretability at Anthropic (09_05_2026 21：25：29).html' 'Presidential Unsealing and Reporting System for UAP Encounters _ U.S. Department of War.html'
powershell -ExecutionPolicy Bypass -File .\scripts\windows\run_attached_html_target_bundle_validation.ps1 -PreferredInitialPage 'Control your online safety and privacy – Google Safety Centre (09_05_2026 21：23：40).html' -InputPath 'Control your online safety and privacy – Google Safety Centre (09_05_2026 21：23：40).html' 'Job Application for [Expression of Interest] Research Manager, Interpretability at Anthropic (09_05_2026 21：25：29).html' 'Presidential Unsealing and Reporting System for UAP Encounters _ U.S. Department of War.html' -Wait
powershell -ExecutionPolicy Bypass -File .\scripts\windows\show_google_issue3_attached_html_target_bundle_proof_entrypoint.ps1 -InputPath 'Control your online safety and privacy – Google Safety Centre (09_05_2026 21：23：40).html' 'Job Application for [Expression of Interest] Research Manager, Interpretability at Anthropic (09_05_2026 21：25：29).html' 'Presidential Unsealing and Reporting System for UAP Encounters _ U.S. Department of War.html'
```

Use that route when the builder-attached files are still the canonical compatibility bundle and the replay does not need to widen back into the broader route-selection notes first.

## Widen back out when needed

If the pinned bundle route turns green or if the next failure should reopen the broader attached-page helpers, keep these nearby:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\windows\show_attached_html_validation_flow.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\windows\show_google_attached_html_validation_flow.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\windows\show_google_issue3_replay_shortcuts.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\windows\show_google_issue3_safe_route_entrypoints.ps1
```

Use the broader attached-page flow when the bundle route should stop being the narrowest branch.
Use the Google-shaped attached-page flow when the next failure still looks closest to the issue `#3` Google follow-up lane.
Use replay shortcuts or the safe-route map only after the pinned bundle route has made the next failure state clear.

## Missing-bundle fallback

If the helper cannot resolve one or more of the three expected builder-attached files automatically, do not treat that as a browser regression first. Reopen the attached-pages harness and print the current manifest surface instead:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\windows\start_attached_pages_catalog.ps1 -GoogleStyle -PrintManifest
powershell -ExecutionPolicy Bypass -File .\scripts\windows\show_attached_pages_preflight_report.ps1 -GoogleStyle
```

Use that fallback when the current workspace no longer matches the pinned builder-attached bundle and the next replay should rediscover the saved-page set honestly before narrowing again.

## Keep these nearby

- `docs/ISSUE3_WINDOWS_REPLAY_ATTACHED_HTML_QUICKSTART.md`
- `docs/ISSUE3_ATTACHED_HTML_TARGET_BUNDLE_REFERENCE.md`
- `docs/ISSUE3_ATTACHED_HTML_TARGET_BUNDLE_QUICKSTART.md`
- `docs/ISSUE3_ATTACHED_HTML_TARGET_BUNDLE_PROOF_ENTRYPOINT.md`
- `docs/ISSUE3_ATTACHED_HTML_TARGET_BUNDLE_CHECKLIST.md`
- `docs/ISSUE3_GOOGLE_ATTACHED_HTML_VALIDATION_FLOW.md`
- `tmp-browser-smoke/attached-pages/README.md`

## Practical rule

Prefer `show_google_issue3_builder_attached_bundle_entrypoint.ps1` whenever the current work should stay pinned to the exact three builder-attached compatibility pages that came with this environment. It is the smallest honest entrypoint for reusing the saved bundle across the replay-attached quickstart, the bundle-first route, the delegated bundle runner, and the proof follow-up without retyping or re-deriving the current filenames.