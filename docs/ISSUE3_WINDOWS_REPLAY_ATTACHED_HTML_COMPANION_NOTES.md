# Issue #3 Windows Replay Attached HTML Companion Notes

Use this note when `show_google_issue3_windows_replay_attached_html_quickstart.ps1` is already open and you want the shortest written map of which nearby notes to keep beside that replay-side attached localhost ladder.

## Read-first helper

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\windows\show_google_issue3_windows_replay_attached_html_quickstart.ps1
```

If a replay already carries `RepoRoot`, `SummaryPath`, or pinned attached bundle paths, keep that same context on the helper first and only then widen back into the written notes.

If the route is reopening from the broader Windows-first branch or from replay shortcuts, rerun the linked surface checks before widening back into the written note family:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\windows\check_google_issue3_windows_replay_attached_html_quickstart_validation_surface.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\windows\check_google_issue3_windows_full_use_attached_html_route_validation_surface.ps1
```

## Keep These Notes Nearby

- `docs/ISSUE3_WINDOWS_REPLAY_QUICKSTART.md` when the replay is still being reopened from the broader replay quickstart before the narrower attached-html lane takes over
- `docs/ISSUE3_REPLAY_SHORTCUTS_WINDOWS_REPLAY_ATTACHED_HTML_BRIDGE.md` when replay shortcuts are still the active surface and you want the compact written bridge into the Windows replay attached-html ladder before the route narrows again
- `docs/ISSUE3_WINDOWS_FULL_USE_ATTACHED_HTML_ROUTE.md` when the replay started from `docs/WINDOWS_FULL_USE.md` and you want the broader Windows-first route plus its route-level surface checker reopened before the narrower replay-side notes take over
- `docs/ISSUE3_WINDOWS_FULL_USE_VALIDATION_ROUTER_ATTACHED_HTML_BRIDGE.md` when the broader Windows-first route should keep the route-level surface check, the validation-router attached-html handoff, and the newer Windows-first catalog step visible before the replay-side ladder narrows again
- `docs/ISSUE3_WINDOWS_FULL_USE_ATTACHED_HTML_CATALOG_QUICKSTART.md` when the Windows full-use branch should keep the compact catalog-side attached-page route visible beside the narrower replay-side chain
- `docs/ISSUE3_VALIDATION_ROUTER_ATTACHED_HTML_QUICKSTART.md` when the broader validation router already narrowed the replay toward the attached localhost branch
- `docs/ISSUE3_ATTACHED_HTML_CHANGE_AREA_QUICKSTART.md` when the replay is already reopening from `show_headed_validation_suites.ps1 -ChangeArea attached-html` and you still want the broader attached-page flow helper kept visible
- `docs/ISSUE3_TOP_LEVEL_ATTACHED_HTML_QUICKSTART.md` when the next run needs the shortest top-level attached-page bridge after the replay-side ladder
- `docs/ISSUE3_TOP_LEVEL_ATTACHED_HTML_BRIDGE.md` when the broader top-level attached-page helper chain still matters after the replay-side ladder is out of the way
- `docs/ISSUE3_TOP_LEVEL_ATTACHED_HTML_CATALOG_QUICKSTART.md` when the compact top-level attached-page route and the suite-catalog-side bridge should stay visible together beside the replay-side ladder
- `docs/ISSUE3_TOP_LEVEL_ATTACHED_HTML_COMPANION_NOTES.md` when you want the matching compact note map for the top-level attached-page helper family once the replay-side ladder hands off
- `docs/ISSUE3_SUITE_CATALOG_TOP_LEVEL_ATTACHED_HTML_CATALOG_QUICKSTART.md` when the suite-catalog-to-top-level attached-page catalog bridge should stay visible beside the replay-side and top-level attached-page helpers before the route narrows again
- `docs/ISSUE3_TOP_LEVEL_SHORTCUT_BRIDGE.md` when the top-level suite router already made issue `#3` obvious and the shortcut-first bridge should stay visible beside the replay-side attached-page chain
- `docs/ISSUE3_SUITE_CATALOG_ENTRYPOINTS.md` when the broader suite-catalog surface should stay visible beside the replay-side chain before the replay narrows again
- `docs/ISSUE3_SUITE_CATALOG_ATTACHED_HTML_BRIDGE.md` when the suite-catalog-side attached-page bridge should stay visible before the replay narrows into shortcuts or safe-route helpers
- `docs/ISSUE3_SUITE_ROUTER_ATTACHED_HTML_QUICKSTART.md` when the route has already narrowed back to the suite-router side and you want that narrower attached-page bridge visible after the replay-side ladder
- `docs/ISSUE3_SUITE_ROUTER_SHORTCUT_BRIDGE.md` when the route is about to narrow from the attached-page chain into the shorter suite-router shortcut-first helper surface
- `docs/ISSUE3_REPLAY_ROUTE_SHORTCUT_BRIDGE.md` when the attached-page route is already clear and you want the narrower replay-route companion visible before widening into replay shortcuts or the safe-route map
- `docs/ISSUE3_REPLAY_DISCOVERY_HANDOFF.md` when the attached-page route is about to widen back into replay shortcuts or the broader issue `#3` handoff map
- `docs/ISSUE3_ATTACHED_HTML_TARGET_BUNDLE_REFERENCE.md` when explicit bundle paths are already pinned and the next replay should stay on the known three-page compatibility set
- `docs/ISSUE3_WINDOWS_VALIDATION_CHAIN.md` when the replay-side attached-page route is done and the next step needs the broader wrapper-heavy checkpoints again

## Practical Rule

1. Start from the replay-side attached-page helper.
2. Reopen `docs/ISSUE3_REPLAY_SHORTCUTS_WINDOWS_REPLAY_ATTACHED_HTML_BRIDGE.md` when replay shortcuts already made the narrower Windows replay attached-html lane obvious and you want that bridge written back down before the replay-side quickstart takes over.
3. Reopen `docs/WINDOWS_FULL_USE.md`, rerun the route-level surface check, and keep the Windows full-use validation-router bridge plus the Windows full-use attached-html catalog quickstart nearby when the broader Windows-first branch should be written back down before the replay-side chain narrows further.
4. Reopen the validation-router attached HTML quickstart or the attached-html change-area quickstart when the broader attached-page route still needs to stay visible before the replay-side ladder hands off to the top-level notes.
5. Keep the top-level attached HTML quickstart, the top-level attached HTML bridge, the top-level attached HTML catalog quickstart, the top-level attached HTML companion notes, the suite-catalog top-level attached HTML catalog quickstart, the suite-catalog entrypoints guide, the suite-catalog attached HTML bridge, and the top-level shortcut bridge nearby when the route should stay visible on the top-level and catalog side before the replay narrows again.
6. Reopen `docs/ISSUE3_SUITE_ROUTER_ATTACHED_HTML_QUICKSTART.md` or `docs/ISSUE3_SUITE_ROUTER_SHORTCUT_BRIDGE.md` when the route has already narrowed from the replay-side chain into the shorter suite-router-side helper family.
7. Reopen the replay-route shortcut bridge, the replay discovery handoff, or the attached-html target bundle reference only after the replay-side ladder has narrowed as far as it can go without widening back into replay shortcuts, bundle-first flow, or the safe-route map.
8. Return to the full validation chain only when the next step needs the broader wrapper-heavy safe route again.
